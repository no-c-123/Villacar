 ---
import { getFeaturedCars, cars, type Car } from '../data/cars';

const featuredCars = getFeaturedCars();
---

<main class="bg-white">
  <!-- Hero Section -->
  <section class="relative min-h-[700px] flex items-center justify-center overflow-hidden">
    <!-- Background Image with Parallax Effect -->
    <div class="absolute inset-0 bg-gradient-to-r from-gray-900/95 to-gray-900/75 z-10"></div>
    <div class="absolute inset-0 bg-cover bg-center transform scale-105" style="background-image: url('https://images.unsplash.com/photo-1552519507-da3b142c6e3d?w=1920&q=80');"></div>

    <div class="relative z-20 container mx-auto px-4 text-white py-20">
      <!-- Main Hero Content -->
      <div class="max-w-4xl mx-auto text-center mb-12">
        <div class="inline-block mb-4 px-4 py-2 bg-red-600/90 backdrop-blur-sm rounded-full text-sm font-semibold">
          ⭐ Serving McAllen Since 2001
        </div>
        
        <h1 class="text-5xl md:text-7xl font-bold mb-6 leading-tight">
          Find Your Perfect <br/>
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-400 to-red-600">Vehicle Today</span>
        </h1>
        
        <p class="text-xl md:text-2xl text-gray-200 mb-10 max-w-3xl mx-auto">
          Quality vehicles from trusted brands like Nissan, Dodge, GMC, and Toyota. Expert service, competitive financing, and honest deals.
        </p>

        <!-- Search Bar -->
        <div class="bg-white rounded-2xl shadow-2xl p-6 mb-8">
          <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <select class="px-4 py-3 rounded-lg border border-gray-300 text-gray-700 focus:outline-none focus:ring-2 focus:ring-red-500">
              <option>All Makes</option>
              <option>Toyota</option>
              <option>Nissan</option>
              <option>GMC</option>
              <option>Dodge</option>
            </select>
            
            <select class="px-4 py-3 rounded-lg border border-gray-300 text-gray-700 focus:outline-none focus:ring-2 focus:ring-red-500">
              <option>All Types</option>
              <option>Sedan</option>
              <option>SUV</option>
              <option>Truck</option>
            </select>
            
            <select class="px-4 py-3 rounded-lg border border-gray-300 text-gray-700 focus:outline-none focus:ring-2 focus:ring-red-500">
              <option>Max Price</option>
              <option>Under $25,000</option>
              <option>Under $35,000</option>
              <option>Under $50,000</option>
              <option>$50,000+</option>
            </select>
            
            <a 
              href="/inventory" 
              class="px-8 py-3 bg-red-600 text-white rounded-lg hover:bg-red-700 transition-all duration-300 font-semibold flex items-center justify-center"
            >
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              Search
            </a>
          </div>
        </div>

        <!-- CTA Buttons -->
        <div class="flex flex-wrap justify-center gap-4">
          <a 
            href="/inventory" 
            class="group px-8 py-4 bg-red-600 text-white rounded-lg hover:bg-red-700 transition-all duration-300 font-semibold text-lg shadow-lg hover:shadow-xl flex items-center"
          >
            Browse All Vehicles
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
          
          <a 
            href="/contact" 
            class="px-8 py-4 bg-white/10 backdrop-blur-md border-2 border-white/50 text-white rounded-lg hover:bg-white hover:text-gray-900 transition-all duration-300 font-semibold text-lg"
          >
            Get Pre-Approved
          </a>
        </div>
      </div>

      <!-- Quick Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto mt-16">
        <div class="text-center bg-white/10 backdrop-blur-md rounded-xl p-6 border border-white/20">
          <div class="text-4xl font-bold text-red-400 mb-2">{cars.length}</div>
          <div class="text-sm text-gray-300">Vehicles Available</div>
        </div>
        <div class="text-center bg-white/10 backdrop-blur-md rounded-xl p-6 border border-white/20">
          <div class="text-4xl font-bold text-red-400 mb-2">24+</div>
          <div class="text-sm text-gray-300">Years Experience</div>
        </div>
        <div class="text-center bg-white/10 backdrop-blur-md rounded-xl p-6 border border-white/20">
          <div class="text-4xl font-bold text-red-400 mb-2">10K+</div>
          <div class="text-sm text-gray-300">Happy Customers</div>
        </div>
        <div class="text-center bg-white/10 backdrop-blur-md rounded-xl p-6 border border-white/20">
          <div class="text-4xl font-bold text-red-400 mb-2">5★</div>
          <div class="text-sm text-gray-300">Customer Rating</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Cars Section -->
  <section class="py-20 bg-gradient-to-b from-gray-50 to-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-block mb-4 px-4 py-2 bg-red-100 text-red-600 rounded-full text-sm font-semibold">
          🚗 Latest Arrivals
        </div>
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Featured Vehicles</h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">Handpicked quality vehicles ready for their next owner</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
        {featuredCars.map((car) => (
          <a href={`/cars/${car.id}`} class="group">
            <div class="bg-white rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-500 transform hover:-translate-y-2">
              <div class="relative overflow-hidden h-56">
                <img 
                  src={car.images[0]} 
                  alt={`${car.make} ${car.model}`}
                  class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                
                {/* Condition Badge */}
                <div class="absolute top-4 right-4">
                  <span class={`px-4 py-2 rounded-full text-xs font-bold shadow-lg backdrop-blur-sm ${
                    car.condition === 'New' ? 'bg-green-600 text-white' :
                    car.condition === 'Certified' ? 'bg-blue-600 text-white' :
                    'bg-gray-700 text-white'
                  }`}>
                    {car.condition}
                  </span>
                </div>

                {/* Quick View Overlay */}
                <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <span class="px-6 py-3 bg-white text-red-600 rounded-full font-bold shadow-xl transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
                    View Details →
                  </span>
                </div>
              </div>
              
              <div class="p-5">
                <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-red-600 transition-colors">
                  {car.year} {car.make} {car.model}
                </h3>
                
                <div class="flex items-center text-sm text-gray-600 mb-4 space-x-4">
                  <span class="flex items-center">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    {car.mileage.toLocaleString()} mi
                  </span>
                  <span class="flex items-center">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                    </svg>
                    {car.fuelType}
                  </span>
                </div>

                <div class="flex justify-between items-center pt-4 border-t border-gray-100">
                  <div>
                    <p class="text-xs text-gray-500 mb-1">Starting at</p>
                    <span class="text-2xl font-bold text-red-600">
                      ${car.price.toLocaleString()}
                    </span>
                  </div>
                  <div class="flex items-center text-red-600 font-semibold">
                    <span class="group-hover:translate-x-1 transition-transform">→</span>
                  </div>
                </div>
              </div>
            </div>
          </a>
        ))}
      </div>

      <div class="text-center">
        <a 
          href="/inventory" 
          class="group inline-flex items-center px-10 py-4 bg-red-600 text-white rounded-xl hover:bg-red-700 transition-all duration-300 font-bold text-lg shadow-lg hover:shadow-xl"
        >
          View All {cars.length} Vehicles
          <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Why Choose Us Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-block mb-4 px-4 py-2 bg-red-100 text-red-600 rounded-full text-sm font-semibold">
          ⭐ Why Choose Us
        </div>
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Why Choose Villacar Autos LLC.</h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">Your trusted dealership for quality vehicles and service</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="group text-center p-8 rounded-2xl bg-gradient-to-br from-red-50 to-white hover:shadow-2xl transition-all duration-300 border border-red-100 hover:border-red-200">
          <div class="w-20 h-20 bg-gradient-to-br from-red-600 to-red-700 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Safe & Reliable</h3>
          <p class="text-gray-600">Every vehicle thoroughly inspected for safety and reliability</p>
        </div>

        <div class="group text-center p-8 rounded-2xl bg-gradient-to-br from-red-50 to-white hover:shadow-2xl transition-all duration-300 border border-red-100 hover:border-red-200">
          <div class="w-20 h-20 bg-gradient-to-br from-red-600 to-red-700 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Great Financing</h3>
          <p class="text-gray-600">Flexible financing options to fit your budget</p>
        </div>

        <div class="group text-center p-8 rounded-2xl bg-gradient-to-br from-red-50 to-white hover:shadow-2xl transition-all duration-300 border border-red-100 hover:border-red-200">
          <div class="w-20 h-20 bg-gradient-to-br from-red-600 to-red-700 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Expert Team</h3>
          <p class="text-gray-600">Knowledgeable staff ready to help you find the right car</p>
        </div>

        <div class="group text-center p-8 rounded-2xl bg-gradient-to-br from-red-50 to-white hover:shadow-2xl transition-all duration-300 border border-red-100 hover:border-red-200">
          <div class="w-20 h-20 bg-gradient-to-br from-red-600 to-red-700 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Customer First</h3>
          <p class="text-gray-600">Your satisfaction is our top priority</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="py-20 bg-gradient-to-b from-gray-50 to-gray-100">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-block mb-4 px-4 py-2 bg-red-100 text-red-600 rounded-full text-sm font-semibold">
          💬 Customer Reviews
        </div>
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">What Our Customers Say</h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">Real experiences from real customers</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300">
          <div class="flex items-center mb-4">
            {[...Array(5)].map((_, i) => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            ))}
          </div>
          <p class="text-gray-700 mb-6 italic">"Great experience! The team helped me find the perfect truck for my business. Honest, fair pricing and excellent service."</p>
          <div class="flex items-center">
            <div class="w-12 h-12 bg-red-600 rounded-full flex items-center justify-center text-white font-bold text-lg">
              JM
            </div>
            <div class="ml-4">
              <p class="font-bold text-gray-900">Juan Martinez</p>
              <p class="text-sm text-gray-600">2024 GMC Sierra Owner</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300">
          <div class="flex items-center mb-4">
            {[...Array(5)].map((_, i) => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            ))}
          </div>
          <p class="text-gray-700 mb-6 italic">"Bought my daughter's first car here. They were so patient and helpful. The car is reliable and safe. Highly recommend!"</p>
          <div class="flex items-center">
            <div class="w-12 h-12 bg-red-600 rounded-full flex items-center justify-center text-white font-bold text-lg">
              MG
            </div>
            <div class="ml-4">
              <p class="font-bold text-gray-900">Maria Garcia</p>
              <p class="text-sm text-gray-600">2023 Toyota Camry Owner</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300">
          <div class="flex items-center mb-4">
            {[...Array(5)].map((_, i) => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            ))}
          </div>
          <p class="text-gray-700 mb-6 italic">"Best car buying experience! No pressure, transparent pricing, and they worked with my budget. Love my new Nissan!"</p>
          <div class="flex items-center">
            <div class="w-12 h-12 bg-red-600 rounded-full flex items-center justify-center text-white font-bold text-lg">
              RC
            </div>
            <div class="ml-4">
              <p class="font-bold text-gray-900">Roberto Cruz</p>
              <p class="text-sm text-gray-600">2024 Nissan Rogue Owner</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Recently Viewed Section -->
  <section id="recentlyViewedSection" class="py-20 bg-white hidden">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <div class="inline-block mb-4 px-4 py-2 bg-blue-100 text-blue-600 rounded-full text-sm font-semibold">
          👁️ Your History
        </div>
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Recently Viewed</h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">Pick up where you left off</p>
      </div>

      <div id="recentlyViewedGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Will be populated by JavaScript -->
      </div>
    </div>
  </section>

  <!-- Call to Action Section -->
  <section class="py-20 bg-gradient-to-br from-gray-900 to-gray-800 relative overflow-hidden">
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-0 left-0 w-96 h-96 bg-red-600 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 right-0 w-96 h-96 bg-red-600 rounded-full blur-3xl"></div>
    </div>
    
    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
          Ready to Drive Home Your Perfect Vehicle?
        </h2>
        <p class="text-xl text-gray-300 mb-10">
          Visit us today or browse our complete inventory online. Our team is ready to help you get on the road!
        </p>
        <div class="flex flex-wrap justify-center gap-4">
          <a 
            href="/inventory" 
            class="px-10 py-4 bg-red-600 text-white rounded-xl hover:bg-red-700 transition-all duration-300 font-bold text-lg shadow-lg hover:shadow-2xl"
          >
            Browse All Vehicles
          </a>
          <a 
            href="/contact" 
            class="px-10 py-4 bg-white text-gray-900 rounded-xl hover:bg-gray-100 transition-all duration-300 font-bold text-lg shadow-lg"
          >
            Get Pre-Approved
          </a>
        </div>
        
        <div class="mt-12 pt-12 border-t border-white/20">
          <p class="text-white font-semibold mb-4">Visit Us Today</p>
          <p class="text-gray-300 text-lg">4208 S 23rd St, McAllen, TX 78503</p>
          <p class="text-gray-300 text-lg mt-2">Mon-Fri: 9AM - 6PM | Sat: 10AM - 4PM</p>
          <a href="tel:5551234567" class="inline-block mt-4 text-red-400 hover:text-red-300 font-bold text-xl">
            📞 (555) 123-4567
          </a>
        </div>
      </div>
    </div>
  </section>
</main>

<script define:vars={{ cars }}>
  // Recently Viewed functionality
  function loadRecentlyViewed() {
    const recentlyViewed = JSON.parse(localStorage.getItem('recentlyViewed') || '[]');
    
    if (recentlyViewed.length === 0) {
      return;
    }

    const section = document.getElementById('recentlyViewedSection');
    const grid = document.getElementById('recentlyViewedGrid');
    
    section?.classList.remove('hidden');

    const viewedCars = recentlyViewed
      .map(id => cars.find(c => c.id === id))
      .filter(Boolean)
      .slice(0, 4);

    if (grid) {
      grid.innerHTML = viewedCars.map(car => `
        <a href="/cars/${car.id}" class="group">
          <div class="bg-white rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-500 transform hover:-translate-y-2">
            <div class="relative overflow-hidden h-56">
              <img 
                src="${car.images[0]}" 
                alt="${car.make} ${car.model}"
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              
              <div class="absolute top-4 right-4">
                <span class="px-4 py-2 rounded-full text-xs font-bold shadow-lg backdrop-blur-sm ${
                  car.condition === 'New' ? 'bg-green-600 text-white' :
                  car.condition === 'Certified' ? 'bg-blue-600 text-white' :
                  'bg-gray-700 text-white'
                }">
                  ${car.condition}
                </span>
              </div>

              <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                <span class="px-6 py-3 bg-white text-red-600 rounded-full font-bold shadow-xl transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
                  View Details →
                </span>
              </div>
            </div>
            
            <div class="p-5">
              <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-red-600 transition-colors">
                ${car.year} ${car.make} ${car.model}
              </h3>
              
              <div class="flex items-center text-sm text-gray-600 mb-4 space-x-4">
                <span class="flex items-center">
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  ${car.mileage.toLocaleString()} mi
                </span>
                <span class="flex items-center">
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                  </svg>
                  ${car.fuelType}
                </span>
              </div>

              <div class="flex justify-between items-center pt-4 border-t border-gray-100">
                <div>
                  <p class="text-xs text-gray-500 mb-1">Starting at</p>
                  <span class="text-2xl font-bold text-red-600">
                    $${car.price.toLocaleString()}
                  </span>
                </div>
                <div class="flex items-center text-red-600 font-semibold">
                  <span class="group-hover:translate-x-1 transition-transform">→</span>
                </div>
              </div>
            </div>
          </div>
        </a>
      `).join('');
    }
  }

  loadRecentlyViewed();
</script>
